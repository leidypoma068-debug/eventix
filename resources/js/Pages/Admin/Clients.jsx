import { Head } from '@inertiajs/react';
import { useMemo,useState } from 'react';
import AdminLayout from '../../Layouts/AdminLayout';
import { Badge, Card, PageTitle, money } from '../../Components/AdminUi';
export default function Clients({clients=[]}){const[q,setQ]=useState('');const rows=useMemo(()=>clients.filter(c=>`${c.name} ${c.email} ${c.phone||''}`.toLowerCase().includes(q.toLowerCase())),[clients,q]);return <AdminLayout><Head title="Clientes"/><div className="mx-auto max-w-[1600px] p-5 sm:p-8"><PageTitle title="Base de clientes" description="Consulta nombre, correo, teléfono, cantidad de compras, entradas, gasto acumulado y último acceso."/><Card className="mt-6 p-5"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar cliente..." className="w-full rounded-xl border-slate-200"/></Card><Card className="mt-5 overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-400"><tr><th className="px-6 py-4">Cliente</th><th>Teléfono</th><th>Compras</th><th>Entradas</th><th>Gastado</th><th>Último acceso</th><th>Registro</th><th>Estado</th></tr></thead><tbody>{rows.map(c=><tr key={c.id} className="border-t border-slate-100"><td className="px-6 py-4"><p className="font-black text-slate-800">{c.name}</p><p className="text-xs text-slate-400">{c.email}</p></td><td>{c.phone||'—'}</td><td>{c.purchases}</td><td className="font-black text-violet-700">{c.tickets}</td><td>{money(c.spent)}</td><td>{c.lastAccess||'—'}</td><td>{c.createdAt||'—'}</td><td><Badge tone={c.active?'green':'red'}>{c.active?'Activo':'Inactivo'}</Badge></td></tr>)}</tbody></table></div></Card></div></AdminLayout>}
